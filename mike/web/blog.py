# import sqlite3
#
# connection = sqlite3.connect('db.sqlite3')
#
# cursor = connection.cursor()
#
# cursor.execute('''CREATE TABLE users (id int auto_increment primary key, date date, name varchar)''')
#
# cursor.execute("INSERT INTO users (date,name) VALUES ('2017/10/27','penza_rugby_players_were_winners')")
#
# connection.commit()
#
# cursor.close()
# connection.close()
